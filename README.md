# 🌊 SeaVerse AI 搜索原型

基于用户历史的智能技能匹配系统

---

## 📦 项目说明

这是一个 AI 搜索原型，展示如何根据用户需求自动选择合适的 AI 技能（图像生成、视频生成、音频生成）。

**当前状态：使用 Mock API（测试数据）**

---

## 🚀 快速开始（Replit 部署）

### 1. 导入项目到 Replit

1. 访问 [Replit](https://replit.com)
2. 点击 "Create Repl" → "Import from GitHub"
3. 输入你的 GitHub 仓库地址：`https://github.com/89699203-art/seaverse-api-server`
4. 点击 "Import from GitHub"

### 2. 安装依赖

在 Replit Shell 中运行：

```bash
npm install
```

### 3. 启动服务

在 Replit Shell 中运行：

```bash
npm start
```

或者直接点击 Replit 的 "Run" 按钮

### 4. 访问应用

- 打开 Replit 提供的公开 URL
- 你会看到完整的 AI 搜索界面
- 可以输入需求测试（目前返回 Mock 数据）

---

## 📂 项目结构

```
项目根目录/
├── index.html          # 前端页面（AI 搜索界面）
├── server.js           # 统一服务器（前端 + Mock API）
├── package.json        # 项目配置
├── README.md           # 本文档
└── .gitignore          # Git 忽略配置
```

---

## 🔧 功能说明

### 当前功能（Mock 模式）

✅ 完整的 AI 搜索界面
✅ 用户输入需求分析
✅ 智能技能选择（基于历史偏好）
✅ Prompt 自动优化
✅ 历史记录保存
✅ Discover 推荐系统
✅ Mock API 返回测试数据

### 支持的技能类型

- **图像生成**：Seedream 4.5, Gemini 3 Pro
- **视频生成**：Kling v2.6, Wanx 2.5
- **音频生成**：Mureka Song

---

## 🔑 如何替换为真实 API

### 步骤 1：获取 API Keys

联系 SeaVerse 技术团队获取以下 API Keys：

- Gemini API Key
- Kling API Key
- Mureka API Key
- Seedream (Volces) API Key

### 步骤 2：配置环境变量

在 Replit 项目中：

1. 点击左侧 "Tools" → "Secrets"
2. 添加以下环境变量：

```
GEMINI_API_KEY=your_gemini_key
KLING_API_KEY=your_kling_key
MUREKA_API_KEY=your_mureka_key
SEEDREAM_API_KEY=your_seedream_key
```

### 步骤 3：替换 server.js

将 `server.js` 中的 Mock API 部分替换成真实的 API 调用逻辑。

参考本地开发环境中的 `api-server.cjs` 实现。

---

## 🎯 测试场景

在界面输入以下需求测试不同功能：

| 输入示例 | 预期行为 |
|---------|---------|
| "画一只可爱的猫咪" | 选择 Seedream 4.5（可爱风格） |
| "专业产品摄影" | 选择 Gemini 3 Pro（专业风格） |
| "制作产品介绍视频" | 选择 Kling v2.6（视频生成） |
| "生成轻快的流行音乐" | 选择 Mureka Song（音频生成） |
| "产品介绍视频配音乐" | 组合 Kling v2.6 + Mureka Song |

---

## 🐛 常见问题

### Q: 为什么生成的是随机图片/视频？

A: 当前使用 Mock API 模式，返回的是测试数据。需要配置真实 API Keys 才能生成真实内容。

### Q: 如何查看 API 调用日志？

A: 在 Replit Console 中可以看到所有 API 调用的日志输出。

### Q: 如何修改 Mock 数据？

A: 编辑 `server.js` 中的 `mockImages`, `mockVideos`, `mockAudios` 数组。

---

## 📝 开发者说明

### 本地开发

如果需要在本地开发：

```bash
# 克隆项目
git clone https://github.com/89699203-art/seaverse-api-server.git

# 安装依赖
npm install

# 启动服务
npm start

# 访问
打开浏览器访问 http://localhost:3000
```

### 技术栈

- **前端**：原生 HTML + CSS + JavaScript
- **后端**：Node.js + Express
- **数据存储**：LocalStorage（浏览器端）

### API 接口

#### POST /api/generate-image

**请求体：**
```json
{
  "skillId": "api_volces_seedream_4_5",
  "prompt": "一只可爱的猫咪"
}
```

**响应：**
```json
{
  "imageUrl": "https://...",
  "status": "success",
  "isMock": true
}
```

---

## 📧 联系方式

如有问题，请联系 SeaVerse 团队。
